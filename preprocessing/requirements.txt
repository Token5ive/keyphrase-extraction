# KP20k 전처리 모듈 필수 라이브러리
# 설치: pip install -r requirements.txt

# 핵심 라이브러리 (버전 고정 필수!)
datasets==2.19.0       # kp20k 호환 버전 (절대 변경 금지!)
pyarrow==15.0.0        # datasets 2.19.0과 호환되는 버전

# 데이터 처리
pandas>=2.0.0
numpy>=1.20.0
